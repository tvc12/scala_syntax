println("Variable are like values, can re-assign them, define by 'var' keyword")
var x = 1 + 2
println(s"Value is x: $x")

var defaultValue: Int = 4
defaultValue = 1 + 2
println(s"default value $defaultValue")
